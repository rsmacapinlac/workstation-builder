---
# Core package installation

- name: Install core tools
  become: true
  pacman:
    name: "{{item}}"
    state: present
    update_cache: yes
  loop:
    - curl
    - vim
    - base-devel
    - git
    - nodejs
    - npm
    - fastfetch
    - autorandr
    - syncthing 